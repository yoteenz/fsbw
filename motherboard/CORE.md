# Core Context — Build-a-Wig / Frontal Slayer

Canonical reference for stack, design, and main flows. Keep this updated when the project’s **persistent** design or architecture changes.

---

## Stack & repo

- **Frontend:** React 19, TypeScript, Vite 5, React Router 6, Tailwind CSS.
- **Backend / Auth / DB:** Supabase (Auth, profiles, orders, cart, wishlist, key/value **app_config** for site-wide JSON such as the admin special-offer card, **`site_analytics_events`** for marketing/social click analytics ingested via `POST /api/analytics/event`). Vercel serverless API routes under `api/`. Full table definitions, RLS, and `auth.users` → `profiles` trigger: run `supabase/migrations/20260325120000_full_app_sync.sql` in the Supabase SQL Editor when setting up or repairing sync. Marketing/admin JSON: also run `supabase/migrations/20260325140000_app_config_marketing.sql` for the `app_config` table. Analytics: run `supabase/migrations/20260330180000_site_analytics_events.sql` for the events table (Admin Brand / Analytics tabs read via `GET /api/admin/analytics`).
- **Env:** Vite uses `VITE_*` (e.g. `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_API_BASE`, `VITE_ADMIN_EMAILS`). Backend uses `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `ADMIN_EMAILS`. **Stripe membership (optional):** `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID_3MONTHS` / `_6MONTHS` / `_12MONTHS`, `SITE_URL` — see `docs/STRIPE_MEMBERSHIP_SETUP.md`.
- **Local:** `.env.local` for dev; copy from `.env.example`. Restart dev server after changing env.
- **Fal image models (golden picks):** Curated model notes in **`motherboard/golden-models/`**; winning prompts in **`motherboard/golden-prompts/`**. Defaults for this stack: **GPT Image 2** (`openai/gpt-image-2/edit`) — **NOIR live color + styling** previews (`live-noir-color`, `live-wig-after-color-styling`; **3:4** `1536×2048`, `quality: medium` ~2K tier, WebP); **NBP** (`fal-ai/nano-banana-pro/edit`) — lobby/lounge scenes, people, text accuracy; **Ideogram on Fal** — background removal (e.g. PSA avatars). Do not run `scripts/psa-flatten-avatar-backgrounds.mjs` on Ideogram-cut transparent PNGs.
- **NOIR mannequin assets (split):** **UI overlays** = transparent `live-preview/Noir/image (27|26|28).png` (`bawStaticMannequinReferencePaths.ts`). **Fal GPT2** = gray-brick baked `fal-gray-brick-{left|front|right}.png` (`bawNoirFalMannequinUrls.ts`; build via **`npm run wig-preview:build-noir-fal-gray-brick-refs`**). Verify **`npm run wig-preview:verify-noir-mannequins`**. Regen color WebPs after Fal ref changes.
- **PSA premium gate:** Client uses `isPremiumMemberForGatedFeatures()` (localStorage); server reads Supabase `profiles` in `api/_lib/psaPremiumCheck.ts`. Founder admin Rewards **subscription toggles** sync to Supabase via `syncAdminSubscriptionOverrideToSupabase` (`src/utils/adminSubscriptionOverrideSync.ts`). **PSA-only bypass:** `kateenaarmstrong@gmail.com` passes server gate if profile is stale.
- **PSA voice:** Founder hologram — trust over sales, four pillars (concierge / hair bestie / educator / no-gatekeeping). Instructions in **`api/_lib/psaInstructions.ts`**; reference **`motherboard/golden-prompts/psa-founder-voice.md`**. Only recommend real catalog units (NOIR, BLANCO, SOFT WAVE, BEACH WAVE, SOFT CURL, OCEAN CURL).

---

## Design system

- **Fonts:** "Futura PT" family (Book, Medium, Demi). "Covered By Your Grace" for accent numbers/dates. Uppercase labels and CTAs common.
- **Account → Alerts** (`/account/notifications`, `src/pages/account/notifications/page.tsx`): each alert row uses **three** stacked text lines — **(1)** title: **black**, **Covered By Your Grace** (~14px, uppercase); **(2)** body line: **gray** (`#808080`), **Futura PT Demi** (~10px, uppercase); **(3)** action link: **red** (`#EB1C24`), **Futura PT Medium** (~10px, uppercase). Apply this pattern for **all** alerts going forward unless product asks otherwise.
- **Brand red:** `#EB1C24` — primary buttons, links, active states, key icons.
- **Backgrounds:** Marble texture (`/assets/marble-half.png`), white/translucent cards (`bg-white/60`, `backdrop-blur-sm`), borders often `1.3px solid black` or `1.3px solid #9ca3af` (gray).
- **Icons:** SVGs in `public/assets/` (e.g. `special-offer.svg`, `slay-challenge.svg`, `Free Gift.svg`). Use `<img src="/assets/...">` with red filter when they should match brand red.
- **Layout:** Mobile-first; shared nav with back/menu, breadcrumb-style text (e.g. `ACCOUNT > CONCIERGE`), cart icon, dynamic cart count from localStorage / events.
- **Shop / product nav search:** Many storefront pages use **`useShopNavSearchBar`** (`src/components/shop/useShopNavSearchBar.tsx`) built on **`useSiteNavSearch`** (`src/hooks/useSiteNavSearch.ts`). Tapping the magnifier opens a **left-aligned** red **Futura PT Medium** **`?q=`** search field **in place of** the breadcrumb (placeholder + typed text **`#EB1C24`** via **`.site-nav-search-input`**). **Enter** navigates to **`/home/shop?q=...`** (`shopProductSearchHref`). **`src/utils/shopProductSearch.ts`** — intelligent BAW + unit + **gift card** matching (`GIFT_CARD_BALANCE_OPTIONS`); **`ShopSearchResultsGrid`** renders **`/units/straight`** staggered marble cards (wig units + gift cards with **DIGITAL ONLY** line). Non-matching products omitted; crumb **SHOP > SEARCH** when results show.

## ACTIVE BUILD TARGET (IMPORTANT)

- **MOBILE-ONLY RIGHT NOW.** Treat this project as mobile-only for **implementation, manual QA, automated tests (where applicable), and debugging guidance.**
- **Tests and verification are mobile-first by default.** Run checks on a **real phone** (or phone-sized browser with the understanding it is a proxy—not a substitute for device behavior). Do **not** treat desktop Chrome/Firefox as the default “official” test environment unless the user explicitly asks for desktop-only or the work is server/API-only with no UI.
- When explaining how to **test, reproduce, or trace** issues (network, auth, profile sync, etc.), give **mobile-appropriate steps first**—e.g. iPhone + Mac **Safari → Develop → [device] → Web Inspector → Network**, on-device **`?auth_debug=1`** / Auth Debug Panel, or documented in-app diagnostics. Desktop **DevTools → Network** is a **secondary** shortcut, not the primary instruction path for this project.
- Do **not** default to desktop-browser-centric debugging instructions when the user asks for testing or trace steps without specifying desktop.
- Desktop layout/version will be created later as a separate phase.

---

## Auth & identity

- **Sign-in:** Supabase email/password only; no OAuth (Google/Facebook). Users with an email must have **`email_confirmed_at`** set (confirmed via Supabase email) before the app treats them as signed in; otherwise they see **INVALID EMAIL OR PASSWORD** and the session is cleared. **Passwords are never stored** in `localStorage` or auth backup cookies — only Supabase session tokens. Legacy local-only sign-in/bootstrap paths were removed.
- **Build-a-Wig entry (signed-in only):** Unit PDP **CUSTOMIZE IN BUILD-A-WIG**, mobile menu **BUILD-A-WIG**, **cart dropdown** and **shopping bag** **EDIT IN BUILD-A-WIG** require sign-in: **`BuildAWigFeatureSignInModal`** (`FORGOT SOMETHING?` / “You must sign in to access this feature.”). **`returnTo`** for cart/bag edit is the product-specific **`/build-a-wig/.../edit`** route (localStorage for **`editingCartItem`** is set before the modal). **Shopping bag `+ LIST`** (add to wishlist list) uses the same modal when signed out; **`returnTo`** is the current bag URL. **`ShopMobileMenuShopTab`** accepts optional **`onBuildAWigRequiresSignIn`** + **`isSignedInForBuildAWig`**. After sign-in from the hub, **`?bawMenu=1`** opens the menu on **`/build-a-wig/noir`** (strip-once `useEffect`); booking **`BookingFlowLayout`** uses the same gate for **`/build-a-wig/noir`**. **View mode** (`/build-a-wig/view/…` only; legacy **`/build-a-wig/try/…`** redirects): footer **VIEW SUBSCRIPTIONS** + hub **SAVE SLAY CARD** for signed-out, standard, and premium members; option sub-pages use **SAVE SELECTION**. Member customize/edit routes use commerce footers (**ADD TO BAG** / **CONFIRM SELECTION** / **CUSTOMIZE IN BUILD-A-WIG**). Legacy **`/build-a-wig`** redirects — guests → **`/build-a-wig/view`**, signed-in → **`/build-a-wig/noir`**.
- **Sign out from the menu toggle:** Use **`signOutAppAndSupabaseSession()`** (`adminAuth.ts`) — **`markManualSignOutInProgress`**, **`supabase.auth.signOut()`**, **`clearAppAuth()`**, and **`signInStateChanged`**. Clearing only **`localStorage`** left the Supabase session in **`sb-*-auth-token`** + cookies, so **`main.tsx`** **`bootstrapAuthBeforeRender`** re-applied the user on the next navigation.
- **Shopping bag (`/bag`) and checkout:** **`CommerceRouteGuard`** always allows **`/bag`**, **`/checkout`**, **`/checkout/bookings`**, and **`/checkout/gift-card`** so guests can view the bag and pay without a Supabase session. **`CartDropdown`**: when the dropdown closes, it still mounts **`BuildAWigFeatureSignInModal`** so **EDIT IN BUILD-A-WIG** can open the gate after **`onClose()`**. On **`/bag`**, **`BuildAWigFeatureSignInModal`** instances for **EDIT IN BUILD-A-WIG** and **`+ LIST`** are rendered via **`createPortal(..., document.body)`** so overlays sit above page content (same stacking idea as cart).
- **Admin list:** `src/utils/adminAuth.ts`. Default admin emails include `kateenaarmstrong@gmail.com`, `admin@frontalslayer.com`, `kateena.armstrong@frontalslayer.com`. Env overrides: `VITE_ADMIN_EMAILS` (frontend), `ADMIN_EMAILS` (Vercel API). When set, env list is used (include all desired admins, e.g. Supabase emails for each operator).
- **Admin features:** Access to `/admin/*`, “Sync my account” in Account → Settings, Concierge card for founder-privileged admin and premium. Sync uses `POST /api/admin/sync-profile` with email + password (Supabase credentials). **Admin founder** (same as founder-privileged admin): `kateenaarmstrong@gmail.com` — `FOUNDER_PRIVILEGED_ADMIN_EMAIL` / `isAyoteenzAdminAccount` / `isAdminFounderAccount` in `adminAuth.ts`. **ADMIN: FOUNDER** on Account → Profile (tap → `/admin/dashboard`), tier/subscription test toggles, mock loyalty/vouchers, merged mock clients in admin UI. **Admin Meetings demo consults** for the founder account: **ORDER #331** (`adminFounderDemoConsultMeetingOrder331`, id **`demo-baw-consult-order-331`**, 2:00 PM), **ORDER #340** (`adminFounderDemoConsultMeetingOrder340`, **`demo-baw-consult-order-340`**, 3:30 PM — custom / gallery inspo path), **ORDER #341** (`adminFounderDemoConsultMeetingOrder341`, **`demo-baw-consult-order-341`**, 4:45 PM — alternate unit / AI preview path). Merged **after** mock/API/local rows when **`isAdminEmail(currentUser)`** so backend rows cannot replace them; **`listAggregatedAdminMeetingsForClientDetails`** uses the same rule. Dated **first day of the hub’s visible calendar month**. Matching **Account → Orders** rows: **ORDER #331** (COMPLETE + offer snapshot) is injected into **past** when missing; **ORDER #340** and **#341** are **PROCESSING** with **no** pre-built quote/snapshot and are injected into **active** when missing (end-to-end **Send offer** testing; see `mergeFounderAdminConsultOrder331Demo` in `orders/page.tsx`). Consult list search includes **`metadata.orderNumber`** and meeting **`id`**. **Delete account:** only `PROTECTED_FROM_ACCOUNT_DELETION_EMAIL` (same Gmail by default) is blocked client + server; `ayoteenz@yahoo.com` and all other users can delete if the API succeeds (`SUPABASE_SERVICE_ROLE_KEY` required on Vercel).

---

## Key flows

- **Account / Concierge:** Priority messages, order tracking, Slay Challenge (6‑month cycles, reward selection), Special Offer (configurable via Admin → Marketing; $40 off, 60‑day countdown), Free Gift, Birthday Gift. Founder-privileged admin can override Slay Challenge stage for testing.
- **Account → Rewards (premium chart) + subscription checkout:** The premium comparison chart and **CONFIRM SUBSCRIPTION** live on **Account → Rewards** at **`/account/rewards`** (`src/pages/account/membership/page.tsx`). Confirming navigates to **`/checkout/upgrade`** with the tier in `localStorage` (`subscriptionUpgrade`). **Subscribe with card (Stripe)** appears only on **`/checkout/upgrade`** (`src/pages/checkout/page.tsx`), not on Rewards, when **`GET /api/stripe/membership-available`** is **`available: true`** and the user has a **Supabase** session (`getAccessToken()`). Stripe success returns to **`/checkout/upgrade?stripe=success`**, then the app syncs profile and routes to Rewards. There is no separate nav item labeled “Membership.”
- **Admin / Marketing:** Special-offer config at **`/admin/marketing`** (nav: "ADMIN > MARKETING"). Page: product, length, density, texture, lace, hairline (incl. LAGOS + PEAK), color, styling (incl. Bangs combos), add-ons (dropdown with all combinations: NONE, single, BLEACH + PLUCK, etc., via `ADDON_COMBO_OPTIONS`), thumbnail, start date. All dropdown panels use `max-h-48 overflow-y-auto` for scrolling. Save Config and RANDOMIZE (red text) below card; RANDOMIZE fills all options with valid random values. **Persistence:** Save writes `specialOfferAdminConfig` to localStorage and upserts the same JSON to Supabase `app_config` key `special_offer_admin` via **`PUT /api/admin/special-offer-config`** (admin session). Concierge and the admin editor load from **`GET /api/special-offer-config`** first, then fall back to localStorage. **Newsletter tab:** After **SPECIAL OFFERS** — HTML email to clients with `notificationNewsletter` opt-in (Account → Settings); templates + recipient multi-select; **`POST /api/admin/newsletter-send`** (admin session) sends via **Resend** when **`RESEND_API_KEY`** (+ optional **`NEWSLETTER_FROM_EMAIL`**) are set on the server; up to 100 recipients per request (UI chunks larger sends). **Transactional email:** Branded HTML templates in **`api/_lib/email/`** — server-only **`sendEmail({ templateType, recipientEmail, subject?, variables? })`** via Resend; assets in Supabase Storage bucket **`email-assets`** (`npm run email:upload-assets`); **`POST /api/email/send`** (admin or **`EMAIL_SEND_SECRET`**) for previews/sends. Wired triggers: Stripe order confirm, membership welcome, affiliate pending/approve/deny, branded password reset, meeting alerts. See **`docs/EMAIL_SYSTEM.md`**.
- **Cart / bag / wishlist line typography:** Product copy (script name, red RAW, CAP SIZE, price, meta) uses **fixed-height layers** — `CartLineProductTextStack` + `CartLineTextLayer` (`src/components/cart/CartLineProductTextStack.tsx`, `src/utils/cartLineProductLayers.ts`) — with symmetric **2px** vertical padding per row so **NOIR** (22px name) sets rhythm and other units align without per-product `marginTop` hacks. Wishlist uses `WishlistLineProductTextStack` / `WishlistItemCapSizeLine` (cap in `slot="cap"`). Legacy spacing helpers in `cartCapSizeLineMargin.ts` remain for spec checks / normalization (`normalizeCartLineProductName`).
- **Storefront inventory (wig units):** Admin **remaining units** (`getDepletedInventory` in `adminRevenueStats.ts` from `userOrders_*` + optional `adminInventoryOverride`) drive shopper **sold-out** UI for the six unit SKUs (**NOIR**, **BLANCO**, **SOFT WAVE**, **BEACH WAVE**, **SOFT CURL**, **OCEAN CURL**). Shared helpers: `src/utils/productInventoryAvailability.ts` (`isWigUnitSoldOut`, `getLineItemStockStatus`, `attachStockStatusToLineItem`); `useProductInventorySnapshot` + `ProductInventorySync` refresh cart/wishlist `stockStatus` on focus/cart/inventory events. Sold-out **cart/bag/wishlist** lines: **red** `OUT OF STOCK`, price **strikethrough** (`WigLineStockPrice`). **Unit PDP** when sold out: price shown **without** strikethrough (`WigProductPriceDisplay` `soldOutPriceTreatment="normal"`); **ADD TO BAG** → disabled **OUT OF STOCK**; **CUSTOMIZE** → **NOTIFY ME WHEN AVAILABLE** + `StockNotifyModal` (`unitStockNotify.ts` waitlist + Account → Alerts row when email has an account). Restock fires **back-in-stock** account alert via `processUnitStockNotifyWaitlistOnInventoryUpdate` on inventory events.
- **Cart / Wishlist:** Stored in localStorage (global and per-user keys). Synced to backend on sign-in/sync where applicable. **BCF bundle-deal** lines (`bcfBundleDeal`): in the **shopping bag** they use the same controls as **A/C** booking lines — **QTY** label + **×** remove only (no **+ LIST**, no ± stepper, no **SAVE FOR LATER**). If the user **no longer qualifies** for premium-gated features (`isPremiumMemberForGatedFeatures` in `premiumMemberAccess.ts` — active premium subscription and/or **BLACK** spend tier), **`isPremiumGatedCartLine`** rows are **removed**: **bundle-deal**, all **`booking-appointment`** (hair installs), and **`booking-consult`** with **`bookingTier === 'premium'`** (standard consults remain). Same strip runs on **`cartItems`** / **`savedForLater`** load, **`signInStateChanged`** / **`focus`** (`applyStripIneligibleBcfBundleDealsToAllStoredCarts` in **`App.tsx`**), **`CartDropdown`**, **`/checkout`**, **`/checkout/summary`** (localStorage fallback), bag **`loadCartItems` / `loadSavedForLater`**. **`MOVE TO BAG`** from saved is blocked for any **`isPremiumGatedCartLine`** when not premium. **Booking:** **`/booking/premium/consultation`** (and **`/consult`**) — **add to bag** is blocked with **`ConfirmationModal`** unless **`isPremiumMemberForGatedFeatures()`** (same pattern as **`/booking/premium/appointment`** and **`/booking/appointment`**); SHOP menu routes non-premium users to standard **`/booking/consultation`** via **`bookingMenuUsesPremiumPaths()`** in **`bookingMemberRoutes.ts`**. **Build-a-wig:** options under **PREMIUM MEMBERSHIP OPTIONS** (lace, texture, color, hairline, styling, add-ons) require the same gate — hub **`handleOptionSelect`** shows upgrade modal; sub-routes use **`useBuildWigPremiumMembershipStepGate`** (`src/hooks/useBuildWigPremiumMembershipStepGate.tsx`). Categories list: **`buildWigPremiumOptions.ts`**.
- **Checkout order strip (shared with cart dropdown):** Horizontal cart tiles on **`/checkout`** and **`/checkout/summary`** use **`src/utils/checkoutOrderStripDisplay.ts`** — same thumbnail sizes/nudges and title/red line as **`CartDropdown`** (BCF shop lines, unit wigs, gift card, membership tiers). **A/C** = internal shorthand for **appointment + consult** booking cart lines (`booking-appointment`, `booking-consult`); titles **BOOKING** / **CONSULT**, badge thumbs **66px** in an **88px** slot (not the large header badge size).
- **Consult quote checkout codes:** Admin-sent **`CONSULT-*`** codes are validated with **`POST /api/checkout/validate-consult-code`** (signed-in owner, not expired, not redeemed); discount applies to non–booking cart subtotal on **`/checkout`** (not **`/checkout/bookings`**). After a successful order, **`POST /api/checkout/redeem-consult-code`** sets **`consult_quotes.redeemed_at`**. Migration **`20260403140000_consult_quotes_redeemed_at.sql`** adds **`redeemed_at`**. **Account → Orders** archived consult **VIEW OFFER** opens **`ConsultOfferClaimModal`** (thumbnail, breakdown, 72h timer, claim → bag + checkout auto-apply); **`markConsultOrderCompleteAfterQuoteSent`** can persist **`consultOfferSnapshot`** on the order for local/offline. **Admin meeting edits:** **`POST /api/admin/meeting-client-alert`** appends a client **notifications** item (reschedule vs cancel); **`AdminMeetingsHub`** uses **RESCHEDULE** / **CANCEL** buttons after **`PATCH`** notes. **Admin Meetings → Send offer:** dropdown option lists render in a **fixed-position portal** to **`document.body`** (high z-index) so lists are not clipped by the main card; default unit preview uses **`ordersPageUnitThumbnailSrcFromUnitKey`** in **`accountReviewProductThumbnail.ts`** (same 2D asset paths as Account → Orders wig list thumbs); optional **CUSTOM IMAGE:** picker (**`OrderFormFilePicker`**, same as order authorization; admin send-offer uses **`hideInlinePreview`** so only the **~122px** preview thumb shows, not a second large image in the picker). Session draft stores manual upload separately; **`localStorage`** key **`bawAdminConsultOfferSavedThumbnails`** maps **unit + length + density + color + hairline + styling + parting** (`partSelection`) **+ add-ons** (sorted) only → **`data:`** URL when admin taps **SAVE SELECTION** (after choosing a file). **STYLING** category adds **PARTING:** (**MIDDLE** / **LEFT** / **RIGHT**) below the styling salon row; **`partSelection`** is stored on **`consult_quotes.selections`** / cart (**$0** **PARTING** line omitted from persisted price breakdown + UI list). Price breakdown amounts use a leading **`+`** for positive lines (including **UNIT**).
- **Consult-only checkout orders** (`bookingFlowType: 'consult'`): **`PLACED` → `PROCESSING`** after **2 hours** from **`placedAt`** (`CONSULT_PLACED_TO_PROCESSING_MS` in **`consultOrderLifecycle.ts`**); **`COMPLETE`** after **72h** from **`placedAt`** (same file). **Orders / confirm / Concierge** consult tracking bar: **`consultDigitalOrderTrackingBarFillPct`** in **`digitalOrderFulfillment.ts`** — **`PLACED`**: ~**10% → 30%** over the first **2h**; **`PROCESSING`**: **30% → 100%** over the remaining **70h** to the 72h window; **`COMPLETE`**: **100%**.
- **Style analysis (consult add-on + PSA selfie):** **Optional** style analysis add-on on **`/booking/consultation`** — when selected, client uploads **selfie** inside the chosen 1- or 4-pick panel plus **hair inspo**; **1 / 4** comparison options → **$20 / $60** (bundled into consult checkout total with **$40** deposit). Skip add-on = **$40 deposit only** (no selfie). **Consult pipeline:** `POST /api/consult-style-analysis-generate` — preferred path is **manual specs** (`manualSpecs` / `inspoSpecs`) selected for the submitted hair inspo image; OpenAI vision spec mapping remains fallback only. Fal populates the same **1 pick** (`IMG_2554`) / **4 pick** (`IMG_2549`) hairstyle analysis templates (`six_month` / `twelve_month`). **Consult prompt rule:** both 1-pick and 4-pick templates must preserve the photographed inspo hairstyle as the visual source of truth (length, bangs/fringe, curl/crimp/wave pattern, part, silhouette, color saturation); BAW spec labels/refs are guardrails only and must not collapse unique inspo styling into generic layers/none or over-long hair. Long-hair drape should be **one-forward-shoulder**: one forward chest cascade on one side, visible hair routed behind the opposite shoulder/down the back, no equal thick forward curtains, and no all-hair-on-one-shoulder pile. **Consult post-process:** force the bottom fade pass, then use the original subtle mirror reflection below that fade; avoid extra client-panel chrome/bottom-overflow template restoration unless a separate artifact-specific fix is designed. **4 pick:** TOP MATCH = inspo on client; MATCH 02–04 = same inspo hairstyle in **3** alternate colors. **Admin debug:** `/tools/hairstyle-analysis` hair consult tiers show selfie + hair inspo + **Manual specs for this hair inspo**; those TOP MATCH picker values are sent as manual specs. **PSA** premium-only **`FIND MY BEST LOOKS`** — ranked unit upsells; results deliver **within 24 hours**. **Template cards (catalog manifest):** `POST /api/hairstyle-analysis-generate`. Admin Tools debug: **1 pick**, **4 pick**, **hair consult 1 pick**, **hair consult 4 pick**. See **`docs/STYLE_ANALYSIS.md`**.
- **`/checkout/bookings`:** Nav crumb **CHECKOUT > BOOKING** (singular). Checkout logic treats **bookings-only** carts like **digital** for **shipping** (no ship address / calculator / method / delivery UI, no tax or loyalty points on that subtotal, billing required, package protection skipped) via **`checkoutSkipsShipping`** and **`isBookingsOnlyCheckoutState`** in **`utils/bookingCheckout.ts`**.
- **`/checkout/gift-card`:** Isolated gift-card checkout (bag filtered to gift lines). Gift cards use the **digital** 3-stage fulfillment timeline only — **no** order authorization form (forms are for hair-related units / BCF bundles, closures, and frontals only).
- **Orders:** Active/past orders per user; tracking stages with progress. Mock/test orders for admin (e.g. Kateena) for demos.
- **Admin → Pending (cross-device when Supabase + migration applied):** Server tables **`pending_order_forms`**, **`pending_affiliate_submissions`**, **`pending_review_supplemental`**, plus **`reviews`** rows with **`status = pending`**. Admin UI loads **`GET /api/admin/pending-queue`** (service role) and **`PATCH`** approve/decline updates DB + **`profiles.user_submitted_reviews`**, **`profiles.affiliate_submitted_content`**, **`profiles.signed_order_forms`**, **`profiles.review_supplemental_overlay`** (catalog/mock review ids), and **`orders`** where applicable. Clients POST **`/api/client/submissions`** (Bearer) for order form, review, supplemental media, affiliate batch; then **`syncProfileFromApi`** mirrors profile JSON into localStorage (**`clientPendingServerSync`**). **Migrations:** run **`20260413120000_pending_admin_queues.sql`** and **`20260414100000_profiles_review_supplemental_overlay.sql`** in Supabase SQL Editor. Without migration or session token, app falls back to prior **localStorage-only** mock queues.
- **Sync:** Profile/orders/cart/wishlist sync via **`syncAllFromApi`** / **`syncProfileWithToken`** (Bearer session to `/api/admin/sync-profile`). Admin email must be in `VITE_ADMIN_EMAILS` and `ADMIN_EMAILS`. No password is sent from the browser.
- **Lounge TV (lobby salon):** Play on lobby opens **`LoungeTvOverlay`** (curtains, CRT static, hand/remote) — **room chrome unchanged**; only the **black TV panel** content was restructured (2026-07). **Nav tabs:** FEATURED, LEARN, EXPLORE, LIVE, LIBRARY. Weekly items are **`LoungeContentPack`** metadata (`src/components/lounge/loungeTvContentPack.ts`) — video + article + transcript + checklist + products + ticket fields; placeholder seed includes **CUTTING YOUR LACE** (BOTH, 8 MIN / 4 MIN, Lace Mastery, free preview). **UI:** streaming rows/cards (`LoungeTvContentPackCard`, `LoungeTvContentRow`), Featured homepage rows, Learn lesson hub (WATCH EPISODE / READ GUIDE), premium article + video detail views, Live placeholders, Library sections (continue/saved/unlocked/etc. via `src/utils/loungeTvLibrary.ts`). Admin **`lounge_tv_admin`** placements migrate legacy `brand` / `slay-tips` / `watch-learn` / `academy` tab keys to new tabs. Tiles still resolve via **`resolveLoungeTvTiles`** for CMS overrides. **Watch player:** **`LoungeTvWatchLearnPlayer`**. **\*NEW\*** tiles: blur until viewed (`loungeTvViewedTiles.ts`). **Capture guard:** **`useLoungeTvCaptureGuard`** + **`LoungeTvContentProtection`**. **DRM deferred** (see MEMORY 2026-05-29).
- **Admin → THE STUDIO:** **studio os** platform — layered architecture: **`src/studio-os-core/`** (reusable platform logic) → **`src/workspaces/`** (registry + brand implementations) → **`src/workspaces/frontal-slayer/`** (first production workspace). Deprecated shim: **`src/studio-os/index.ts`** re-exports core. Platform identity in `studio-os-core/config/platform.ts`. Entry **`/admin/studio-os`** — workspace picker; default Workspace **FRONTAL SLAYER**. Placeholder workspaces: **`sandbox/`**, **`future-brand/`**, **`future-client/`**. Registry injection via `configureWorkspaceRegistry()`; **`AdminStudioWorkspaceGuard`** loads `workspaces` + **`WorkspaceProvider`** for **`/admin/studio/*`** and **`/admin/studio-os/*` only** — **`AdminGuard`** is auth-only (dashboard never waits on Milestone seeds). Docs: **`docs/studio-os/architecture.md`**, **`workspace-system.md`**, **`core-vs-workspace.md`**, **`docs/frontal-slayer/workspace-implementation.md`**. **`/admin/studio`** redirects to **Mission Control**; **Studio Overview** (grouped nav) at **`/admin/studio/overview`**; legacy hub at **`/admin/studio/hub`**; Executive Command Center at **`/admin/studio/executive-command-center`**. Shared Studio shell: **`AdminStudioLayout`** + **`adminStudioNavigation.ts`** (OVERVIEW / CREATE / VISUALS / PRODUCTION / DISTRIBUTION / INTELLIGENCE / LEGACY / SETTINGS), breadcrumbs, back-to-overview link, **`AdminStudioModuleCard`**. **Social Publishing** (`/admin/studio/social-accounts`) — official OAuth connectors (Meta Graph Instagram/Facebook, TikTok Content Posting API, Pinterest API, X API when available); tokens encrypted server-side only; Distribution Network pack **SOCIAL** tab; admin approval required before publish; **`socialPublishingStudioService`**; migration **`20260704120000_studio_social_publishing.sql`**; see **`docs/STUDIO_SOCIAL_PUBLISHING.md`**. **Legacy System** (`/admin/studio/legacy-system`) — permanent memory & living museum: editorial landing cards, The Museum with archives, Hall of Fame, Studio Awards, talent careers, Vault of Firsts, time capsules, timeline, founder journal (founder-only), **`legacySystemStudioService`**. **Asset Director** (`/admin/studio/asset-director`) — **visual asset management** (Milestone 14): 21:9 hero, gallery/list modes, filters, bulk actions, quick preview; studio detail bundles (versions, videos, cameras, lighting, props, talent portraits, wardrobe views, expressions, poses, materials, relationships, moodboard masonry, version timeline, health cards; metadata collapsed); Content Pack visual picker; placeholder only — **`adminStudioAssetDirectorVisual.ts`**, **`assetDirectorStudioService`**. **Production Builder** (`/admin/studio/production-builder`, optional **`?packId=`**) — **visual production assembly** (Milestone 14.5): three-column workspace (asset library · scene builder · inspector), multi-scene shot builder with drag-reorder, auto prompt assembly from approved assets, output types, brand aspect previews, department status bar, templates, save/duplicate/favorite/archive/version history, **Build Production** demo outputs; syncs scene selection to Content Pack asset store; **OPEN IN PRODUCTION BUILDER** on pack detail; **🎬 ENTER DIRECTOR MODE** CTA; placeholder — **`adminStudioProductionBuilderDemo.ts`**, **`productionBuilderStudioService`**. **Director Mode** (`/admin/studio/director-mode`, **`?draftId=`** from Production Builder) — **cinematic rehearsal** (Milestone 15): four-zone control room (timeline · cinema preview · director controls · production console), live storyboard, script/shot list, camera & lighting simulation, talent/voice/music/graphics controls, readiness score, AI rehearsal simulation, director notes, client preview, snapshots, emergency checklist gating **Generate Production**; **Ask Executive AI Director** panel; syncs to Production Builder draft + Content Packs; placeholder — **`adminStudioDirectorModeDemo.ts`**, **`directorModeStudioService`**. **Executive AI Director** (`/admin/studio/executive-ai-director`) — **strategic intelligence advisor** (Milestone 16): Today's Brief, creative intelligence (searchable), production coaching, brand compliance, prompt review (0–100), executive scorecard, performance forecasts (labeled estimates), trend signal plugins (internal only), recommendations, A/B strategy, content gaps, workspace memory (append-only), executive + production timelines, executive chat; analyzes Production Builder drafts; placeholder — **`adminStudioExecutiveAiDirectorDemo.ts`**, **`executiveAiDirectorStudioService`**. **Campaign Orchestrator** (`/admin/studio/campaign-orchestrator`) — **operational launch planner** (Milestone 17): one business objective → full execution plan; 5-step wizard (identity · audience · schedule · creative direction · generate), 16 campaign types + reusable blueprints, visual timeline, auto task generation with department assignment + dependency chains, Executive AI Director pre-production review, dashboard (progress/readiness/risk, deliverables, approvals, automation rules, what-if scenarios, resource view, smart recommendations with reasoning); human approval gates at production/generation/publishing/scheduling/distribution — never auto-executes; placeholder — **`adminStudioCampaignOrchestratorDemo.ts`**, **`adminStudioCampaignOrchestratorPlan.ts`**, **`campaignOrchestratorStudioService`**. **Mission Control** (`/admin/studio/mission-control`) — **workspace executive HQ** (Milestone 18): landing page after workspace selection; modular dashboard (workspace header · executive brief · mission overview hero · active missions · docked Executive AI Director · department grid · mission timeline · executive calendar · approval center · business health scorecards · quick actions · workspace memory · sticky live activity feed); mission search + smart notifications; placeholder — **`adminStudioMissionControlDemo.ts`**, **`missionControlStudioService`**. **`/admin/studio`** redirects to Mission Control; Executive Command Center preserved at **`/admin/studio/executive-command-center`**. **Blueprint Manager** (`/admin/studio/blueprint-manager`, **`/blueprint-manager/:blueprintId`**) — **Asset Factory foundation** (Milestone 18.5): specification-only blueprint library (16 categories · global + workspace scopes · templates · Weather Studio flagship spec); collapsible visual editor (identity · images · videos · camera · lighting · props · prompt stack · dependencies · validation · relationships · checklist · version history); factory readiness score; approval workflow (draft/review/approved/deprecated/archived); Mission Control + Executive AI Director blueprint review integration; no generation — **`adminStudioBlueprintManagerDemo.ts`**, **`blueprintManagerStudioService`**. **Asset Factory** (`/admin/studio/asset-factory`) — **manufacturing department** (Milestone 19): three synchronized views (Executive Dashboard · Factory Floor · Factory Tour cinematic walkthrough); reads approved blueprints; generation plan with approval gate; simulated manufacturing pipeline (9 departments · QA · versioning · auto-populate Asset Director/Mission Control/EAD); provider abstraction (FAL, GPT Image, Runway, Flux, etc.); queue manager (pause/resume/retry/cancel/reprioritize); job inspector; variation engine; live activity feed; demo simulation only — **`adminStudioAssetFactoryDemo.ts`**, **`adminStudioAssetFactoryPipeline.ts`**, **`adminStudioAssetFactoryProviders.ts`**, **`assetFactoryStudioService`**. **Knowledge Hub** (`/admin/studio/knowledge-hub`, profile/workflow sub-routes) — **living documentation layer** (Milestone 20): subtitle *Learn your Workspace. Understand your production.*; universal ⓘ **Learn This Page** slide-over panel on every studio os page via **`StudioKnowledgeProvider`** in **`AdminStudioLayout`**; per-page guides (purpose · why exists · when to use · best practices · mistakes · related pages · workflows · Owner's Manual chapter · tours); searchable wiki; object profiles (studios · shows · talent · campaigns · products · blueprints · modules · etc.); workflow guides; executive notes (localStorage **`adminStudioKnowledgeHub_v1`**); optional contextual hints; Mission Control knowledge-health strip + Executive AI Director tips panel; demo/placeholder — **`adminStudioKnowledgeHubDemo.ts`**, **`useAdminStudioKnowledgeHubState`**, **`knowledgeHubStudioService`**. **Studio set separation** (global rule): every Studio/Set splits into **Master Studio** (empty reusable environment) · **Reference Scene** (staged example only) · **Set Dressing** · **Talent Layers** (Talent Agency) · **Episode Graphics**; enforced in **`adminStudioSetSeparation.ts`**, Blueprint Manager **`studioSetLayers`**, Asset Director studio detail sections, preview modal layer labels, Asset Factory generation order + Master Studio QA, Production Builder layer stack, Director Mode layer toggles, Knowledge Hub studio profiles; existing Weather Studio live generates **reclassified as Reference Scene** — clean Master marked **Needs Generation**. **Executive Command Center** — CEO headquarters: executive overview cards, Creative Director daily briefing, business/studio/audience/product health, launch/opportunity/risk/decision centers, executive search; **`executiveCommandCenterStudioService`**. **Audience Brain**, **Distribution Network**, **AI Production Engine**, **Production Pipeline** — prior milestones. Service registry includes all studio services.

---

## Conventions

- **Explanations for this product owner:** When walking through setup, architecture, or technical steps, assume the reader may **not** be developer-savvy yet. Prefer **plain language**, short steps, analogies (e.g. shopping list, photo album, hired artist), and **Windows-friendly** notes where relevant; introduce jargon only when needed and define it in one line. Put long checklists in **`docs/`** when appropriate so the chat stays scannable.

- **Desktop perspective panel mapping:** Tilted/acrylic/holographic surfaces use **4-corner quads** (image-normalized 0–1) via **`<PerspectivePanel id="…">`** (`src/components/perspective-panel/`). Central defs: **`src/constants/perspectivePanelConfig.ts`** (reception-left/center/right, curator-tablet, checkout-tablet, signin-tablet, tv-lounge-screen, psa-hologram-screen, extensions-boutique-wall). Warp math: **`PerspectivePanelHost`** + **`quadPerspectiveTransform.ts`** + **`desktopRoomCoverLayout.ts`**. **Debug:** set **`window.PANEL_DEBUG = true`** in DevTools (or **`?panelDebug=1`**) — crimson dashed polygons, draggable corners, floating **PANEL DEBUG** toolbar (select panel, edit all, reset, copy/import JSON, save to **`localStorage.perspectivePanelMap`**). Export approved JSON into config for production; do not rely on localStorage alone. Legacy rect-only **`?panelDebug=1`** alignment for Penthouse account panels remains under **`src/components/desktop-panel-debug/`** until migrated.
- **Desktop room overlays (cover-locked):** NO TEXT desktop heroes are **1915×821** with **`object-fit: cover`** + **`center top`** (`DesktopZoneRoomScene`). **All** overlays on those rooms must use **image-normalized coordinates (0–1)** mapped through **`desktopRoomCoverLayout.ts`** — not raw viewport `%` or `vw`. Flat rects may use **`DesktopRoomCoverRectAnchor`**; perspective surfaces use **`PerspectivePanel`**.
- **Lobby (home) nav:** Neon logo (center) and **Products** (neon-products) → `/home/shop`. Tools (neon-tools) → `/home/tools`. Lounge carousel logo → `/shop/units`. Implemented in `src/pages/lobby/page.tsx` (Products uses the full neon asset as the tap target, not a shifted overlay).
- **App default route:** `/` and the index route use **`HomeLandingRedirect`** (`App.tsx`) — **premium** members (subscription and/or BLACK tier) → **`/lobby`**; **standard** members and guests → **`/home/shop`**. Direct navigation to `/home/shop` or `/lobby` is unchanged.
- **Paths:** Pages under `src/pages/` (e.g. `account/`, `account/settings/`, `account/concierge/`, `admin/`). Shared components in `src/components/`, utils in `src/utils/`.
- **State:** React `useState`/`useEffect`; persistent data in localStorage (`currentUser`, `registeredUsers`, cart, wishlist, per-user keys). Supabase session for server-backed data.
- **API:** Base URL from `VITE_API_BASE`; routes in `api/` (e.g. `api/admin/sync-profile`, `api/delete-account`). CORS and JSON error responses on API routes. **Checkout hardening (partial):** `POST /api/checkout/quote` recomputes USD from cart **identities** (booking, consult, simple unit names) and can return `chargeCurrency` / `chargeAmountMinor` via `api/_lib/pricing/fxRates.ts`; `GET /api/fx/rates` exposes the FX table; `POST /api/stripe/create-product-payment-intent` charges in the shopper’s `chargeCurrency` when the cart is fully server-resolvable; webhook `payment_intent.succeeded` can append an order. Details: `docs/CHECKOUT_SERVER_QUOTE.md`. Full checkout subtotal (tax/shipping/vouchers) is still client-side for display; PI amount is server line-item USD converted to charge currency.
- **Docs:** Project docs in `docs/` (e.g. `ADMIN_SYNC_SUPABASE_EMAIL_WALKTHROUGH.md`, `DELETE_ACCOUNT_API_WALKTHROUGH.md`). **Profiles:** Exact Supabase `profiles` columns and how they map to the API and app (name, photo, socials, birthday, rewards) are in `docs/PROFILES_COLUMNS_AND_APP_MAPPING.md`—use it when fixing sync or account repopulation.
- **Profile image:** **`POST /api/profile-image`** accepts a data URL, uploads to Storage bucket **`profile-images`**, then sets **`profiles.profile_image`** using the **service role** (after **`getAuthUser`** validates the JWT). Client PATCH/queue intentionally omit base64 blobs; persistence requires this upload path so **`GET /api/profile`** returns the URL after sign-in.
- **Admin client details (Orders vs Appointments):** Orders and Appointments tabs share the same card layout (flex, gap 12px), typography (Covered By Your Grace 16px for date, Futura PT Medium 10px/12px for title and secondary text, #EB1C24 / #808080), left 85×85 area (orders: product image + "N ITEMS"; appointments: APT placeholder only, no label below), status pill (height 15px, padding 0 6px, borderRadius 2px, Futura PT Medium 8px). Appointment time is offset with `translateY(-2px)`. Empty state: "NO APPOINTMENTS YET" matches "NO ORDERS YET" styling.
- **Client profile BIRTHDAY row:** Add BIRTHDAY between EMAIL and PHONE. Display format "AUGUST 30, 1989". Fallback order: birthDate → birthMonth+birthDay+birthYear (MM/DD/YYYY) → birthMonth+birthDay (MM/DD) → birthDay only ("Day 15") → "—". See MEMORY 2025-02-28.
- **Main card responsive height:** For the single main content card (border, bg-white/60, backdrop-blur-sm, p-4, mb-2) on list/settings-style pages: use proportional scaling so the card keeps the same visual proportion across viewports. **Wishlist lists** uses `calc(100vh * 520 / 745)` (520px at 745px viewport height). **Account Payment** and **Account Shipping** use `minHeight: calc(100vh * 510 / 900)`. Other pages (Wishlist main, Shopping bag, Admin clients, Account settings, Account menu open) use viewport-minus-fixed (e.g. `calc(100vh - 270px)` or `calc(100dvh - 160px)`). See MEMORY 2025-02-28 (main card responsiveness).
- **Branch policy (user preference — critical):** Do **all** implementation work on **`master`** only. **Never create** `cursor/*`, `feature/*`, or other side branches—not when cloud agents, PR tools, or task runners suggest it. After committing: **`git push -u origin master`** only — **once per completed task**. **`motherboard/MEMORY.md` must be in the same commit as any code/doc change** (one Vercel deploy per prompt); use **`./scripts/agent-commit.sh`**. Do **not** merge into, fast-forward, or push **`preview/mobile`** unless the user **explicitly** asks. If an old `cursor/*` branch exists on the remote from past tooling, **delete** it after its commits are on **`master`**: **`git push origin --delete <branch>`**. Do **not** open PRs that require new branches unless the user explicitly asks for a PR-based workflow.
- **Studio OS workspace bootstrap (public routes):** Do **not** top-level `import '../workspaces'` from modules loaded on every page (`App.tsx` → `AdminGuard`). **`AdminGuard`** is **auth-only** — no **`WorkspaceProvider`**, no workspace import on **`/admin/dashboard`**. **`AdminStudioWorkspaceGuard`** wraps **`/admin/studio/*`** + **`/admin/studio-os/*`**, **`await`s `ensureWorkspacesBootstrapped()`**, then mounts **`WorkspaceProvider`**. **Do not** auto-run Milestone 30–36 platform demo seeds on workspaces import — export **`bootstrapWorkspacesPlatform()`** and call from **`AdminStudioLayout`** only.
- **Executive Organization (M41):** `/admin/studio/executive-organization` — living leadership org (Founder → CoS → executives → departments → teams → workers). Executives operate as **one leadership team** coordinated by **Chief of Staff**; core in **`src/studio-os-core/executive-organization/`**; links from CoS workspace and Knowledge Graph.
- **Organizational Inheritance (M42):** `/admin/studio/organizational-inheritance` — foundational platform for inheriting organizational genetics (DNA layers, playbooks, executives, knowledge) when creating new companies. Inheritance wizard linked from Workspace Creation Engine; genetic blending, simulator, institutional library, ancestry/timeline, evolution tracking; marketplace architecture prepared (not implemented). Core in **`src/studio-os-core/organizational-inheritance/`**.
- **Strategy Engine (M43):** `/admin/studio/strategy-engine` — defines the game each company is playing; strategy board, profile, initiatives, strategic bets, alignment checks, health score, decision journal, reviews, simulation, strategy inheritance. Connected to CoS (prioritization), Newsroom (page strategy linkage), Leadership DNA, Organizational Inheritance, KG. Workspace profiles seeded for NDXBOOK + Frontal Slayer (reference data only — no FS code changes). Core in **`src/studio-os-core/strategy-engine/`**.
- **Campaign Engine (M44):** `/admin/studio/campaign-engine` — transforms strategy into coordinated execution; campaigns bridge initiatives and production. Campaign workspace, builder, deliverables (newsroom-linked), calendar, intelligence, analytics, health, experiments (Labs), retrospectives, playbooks, inheritance. Connected to Strategy Engine, Newsroom, CoS, Labs, KG. Core in **`src/studio-os-core/campaign-engine/`**.
- **Work Orchestration (M45):** `/admin/studio/work-orchestration` — intelligent execution without manual task management; work packages, activities, dependencies, capacity intelligence, CoS orchestration, founder workspace, timeline, operational health, knowledge integration. Connected to Chief of Staff, Strategy Engine, Campaign Engine, Newsroom, Executive Organization, Studio Intelligence, KG. Core in **`src/studio-os-core/work-orchestration/`**.
- **Distribution Engine (M46):** `/admin/studio/distribution-engine` — global distribution for knowledge assets; channel optimization, platform adaptation, distribution calendar, evergreen engine, collections, audience adaptation, creator marketplace, simulation, performance intelligence, feedback loop, lineage, cross-company distribution. Connected to Strategy Engine, Campaign Engine, Newsroom, CoS, Studio Intelligence, Growth Network, Labs, KG. Core in **`src/studio-os-core/distribution-engine/`**.
- **Reader Graph (M47):** `/admin/studio/reader-graph` — living relationship map between companies and people; reader profiles, journey stages, graph visualization, knowledge interests, behavior intelligence, relationship health, timelines, community clusters, reader intelligence, recommendations, cross-company relationships, creator marketplace pipeline, relationship simulation, privacy controls. Connected to Distribution Engine, Campaign Engine, Strategy Engine, CoS, Studio Intelligence, Newsroom, KG. Core in **`src/studio-os-core/reader-graph/`**.
- **Relationship Engine (M48):** `/admin/studio/relationship-engine` — active relationship OS transforming reader graph into proactive nurturing; relationship workspaces, lifecycle stages, next best actions, health, timeline, intelligence, community engine, communication orchestration, recognition, loyalty intelligence, portfolio relationships, CoS alerts, relationship simulation, institutional learning. Connected to Reader Graph, Distribution Engine, Campaign Engine, Strategy Engine, CoS, Studio Intelligence, KG. Core in **`src/studio-os-core/relationship-engine/`**.
- **Creator Marketplace (M49):** `/admin/studio/creator-marketplace` — intelligent creator business ecosystem (not post-buying marketplace); creator/brand profiles, intelligent matching, career graph, brand deal engine, creator OS dashboard, agency mode, marketplace intelligence, relationship management, simulation, creator academy, talent network integration, career/payment intelligence. Alignment over follower counts. Connected to Relationship Engine, Reader Graph, Campaign Engine, Strategy Engine, Talent Network, CoS, Studio Intelligence, KG. Core in **`src/studio-os-core/creator-marketplace/`**.
- **Ecosystem Marketplace (M50):** `/admin/studio/ecosystem-marketplace` — central exchange for organizational intelligence (not app store); asset profiles, categories, inheritance integration, compatibility simulation, licensing, asset evolution, community contributions, verified marketplace, collaboration, marketplace intelligence, organizational reputation, installed assets, industry collections, cross-company learning. Organizational capability over files. Connected to Organizational Inheritance, Creator Marketplace, Strategy Engine, Campaign Engine, CoS, Studio Intelligence, KG. Core in **`src/studio-os-core/ecosystem-marketplace/`**.
- **Knowledge Asset Engine (M51):** `/admin/studio/knowledge-asset-engine` — foundational object model for Studio OS; every content piece is a living knowledge asset with permanent profile, single source of truth, evolution, lineage, maturity stages, relationships, transformations, intelligence recommendations, monetization, academy paths, executive intelligence, inheritance packages, knowledge health. Institutional memory · knowledge compounds forever. Connected to Distribution Engine, Reader Graph, Relationship Engine, Strategy Engine, Campaign Engine, Organizational Inheritance, Ecosystem Marketplace, CoS, Studio Intelligence, KG. Core in **`src/studio-os-core/knowledge-asset-engine/`**.
- **Company Maturity Engine (M52):** `/admin/studio/company-maturity-engine` — universal organizational onboarding and understanding system; build new or import existing company → same blueprint. Company stages, existing asset inventory, integration center (architecture-ready), 21-domain maturity assessment, organizational scan/diagnostic, architect recommendations (skip/recommend/optimize), company roadmap, timeline, simulations, CoS maturity monitoring. Meets founders where they are — organizational understanding before improvement. Connected to Business Architect, Leadership DNA, Company DNA, Knowledge Graph, Chief of Staff, Studio Intelligence, Organizational Inheritance, Simulation Engine, Knowledge Asset Engine, future architects. Core in **`src/studio-os-core/company-maturity-engine/`**.
- **Brand Architect (M53):** `/admin/studio/brand-architect` — transform validated business into complete living brand; meaning before colors. Brand blueprint (purpose, promise, positioning, mission, vision, values, personality, archetype, voice, tone), verbal identity (naming, taglines, messaging pillars, manifesto, vocabulary, headline/CTA systems), visual identity (logo, color, typography, photography, motion, layout), brand systems (guidelines, design system, standards, social/email/product identity), competitive intelligence, brand simulation, brand health, evolution timeline, Experience Architect handoff. Cohesive systems — never disconnected assets. Connected to Company Maturity Engine, Business Model Engine, Creative DNA, Leadership DNA, CoS, Studio Intelligence, KG. Core in **`src/studio-os-core/brand-architect/`**.
- **Experience Architect (M54):** `/admin/studio/experience-architect` — emotional design for every touchpoint; memorability over usability. Experience blueprint (18 journey stages from first impression to long-term relationship), customer journey map (touchpoints, friction, delight, trust builders, relationship engine links), emotional architecture (curiosity → legacy progression), experience systems (service, support, membership, community, notification, celebration, trust, retention philosophies), micro-experience library, experience simulation, studio intelligence alerts, cross-channel consistency, experience standards, digital architect handoff. Connected to Brand Architect, Company Maturity Engine, Relationship Engine, Reader Graph, CoS, Studio Intelligence, KG. Core in **`src/studio-os-core/experience-architect/`**.
- **Digital Architect (M55):** `/admin/studio/digital-architect` — digital solution architect V2.0; purpose before templates. Immersive experience mode gallery (classic, luxury, immersive, editorial, community, marketplace, enterprise, saas, custom), hybrid architecture, studio intelligence recommendation engine, immersive preview mode, digital ecosystem builder, solution + application architecture, experience inheritance from brand/experience/maturity, design system generation, AI feature architect, digital simulation, implementation roadmap, developer handoff, integration center, launch architect handoff. Not a website builder — unforgettable digital worlds. Connected to Experience Architect, Brand Architect, Company Maturity Engine, CoS, Studio Intelligence, KG. Core in **`src/studio-os-core/digital-architect/`**.
- **Growth Architect (M56):** `/admin/studio/growth-architect` — sustainable growth operating system; transform digital ecosystems into thriving businesses. Growth blueprint (18 pillars), lifecycle (idea → legacy), initiative builder, GTM planner, growth intelligence, simulation, orchestration (strategy, campaign, distribution, relationship, CoS), experiments (KG learning), market intelligence, expansion architect. Not growth at any cost — relationship-driven compound growth. Connected to Digital Architect, Strategy Engine, Campaign Engine, Distribution Engine, Relationship Engine, Reader Graph, Creator Marketplace, CoS, Studio Intelligence, KG. Core in **`src/studio-os-core/growth-architect/`**.
- **Company Genome (M57):** `/admin/studio/company-genome` — living organizational genetics; visual identity and heartbeat of every company inside Studio OS. Genetic layers (company/creative/writing/leadership/operational DNA), living interactive visualization with zoom (portfolio → decision), genetic relationships, evolution timeline, unified organizational health (11 dimensions), resilience trends, organizational fingerprint, genome intelligence, future simulation, cross-company genetics, knowledge flow. Not analytics — watch organizations evolve. Connected to all DNA layers, architect pipeline M52–M56, Knowledge Graph, Reader Graph, Relationship Engine, Chief of Staff, Studio Intelligence, Organizational Inheritance. Core in **`src/studio-os-core/company-genome/`**.
- **Architect Studio (M58 / M58.5):** `/admin/studio/architect-studio` — immersive innovation headquarters; physical heart of Studio OS. **M58:** Five connected studios (business · brand · experience · digital · growth), central collaboration forum, spatial navigation, living workspaces, evolution wall, innovation lab, portfolio campus. **M58.5 Living Headquarters:** Morning arrival lobby · CoS executive briefing · executive presence (no idle characters) · ambient organizational activity · overheard conversations · living architecture · executive availability · organizational rhythm · headquarters culture · memory spaces. Arrive at work — organization already in motion. Connected to all architects M52–M56, Company Genome, Chief of Staff, Studio Intelligence, Knowledge Graph, Organizational Inheritance. Core in **`src/studio-os-core/architect-studio/`** (v1.5 store).
- **Campus Evolution Engine (M59):** `/admin/studio/campus-evolution-engine` — living architectural representation of organizational growth over years and decades. Day one founder studio · architectural progression (startup studio → legacy campus) · organic evolution from achievements · earned spaces (brand pavilion, newsroom tower, knowledge library, etc.) · company memory · living museum · brand inheritance (every company unique) · culture profile · portfolio campus with shared districts · campus intelligence · living environment (construction never finished) · architectural simulation (5/10/20 year previews). Evolve through meaningful progress — not revenue alone. Connected to Architect Studio, Living Headquarters, Company Genome, Reader Graph, Relationship Engine, all architects, CoS, Studio Intelligence, Organizational Inheritance. Core in **`src/studio-os-core/campus-evolution-engine/`**.
- **Founder Walk (M59.5):** `/admin/studio/founder-walk` — emotional spine of every company's campus; legacy system not achievement system. Day one marble pathway · living pathway extending with milestones · architectural memories (sculptures, gardens, bridges, inscriptions — not badges) · memory markers (why, learned, who, advice) · reflection spaces · living landscape · organizational connections to genome/KG/relationships · future generations insights · family legacy (private/shared) · portfolio legacy links · memory intelligence · founder timeline (day one → year twenty → future) · campus integration as central thread. Celebrate growth, character, wisdom — not revenue alone. Connected to Campus Evolution Engine, Architect Studio, Company Genome, Leadership DNA, Relationship Engine, Organizational Inheritance, Studio Intelligence. Core in **`src/studio-os-core/founder-walk/`**.
- **Remembrance Garden (M59.6):** `/admin/studio/remembrance-garden` — most personal and reflective campus space; preserve gratitude not mourning. Dedication spaces (parents, mentors, first employees, supporters, remembered) · living memorials (flower gardens, cherry blossoms, oak trees, reflection pools, pavilions — meaning not status) · memory preservation (reflections, lessons, quotes, optional media) · reflection spaces · living seasons (blooms, maturity, light, weather) · gratitude moments (Studio Intelligence recommendations) · legacy letters (private/scheduled/legacy unlock) · family heritage · future generations culture-through-stories · portfolio remembrance · privacy levels (private/family/executive/organization/public). Peaceful · hopeful · grateful · alive. Connected to Founder Walk, Campus Evolution Engine, Living Headquarters, Company Genome, Leadership DNA, Knowledge Graph, Relationship Engine, Organizational Inheritance, Studio Intelligence. Core in **`src/studio-os-core/remembrance-garden/`**.
- **Founder's Promise (M59.7):** `/admin/studio/founders-promise` — personal north star and emotional foundation; not mission/vision/marketing. Guided reflective writing experience (Studio Intelligence questions) · the promise (write/edit/version/record/dictate · text/audio/video/handwritten) · living promise (original preserved · evolution tracked) · organizational alignment (decisions scored vs promise · conflicts · adjustments) · executive alignment (CoS, Brand/Experience/Digital/Growth architects) · promise reflection moments · promise archive · legacy inheritance letters · privacy levels · campus installation at Founder Walk and Remembrance Garden entrance. Optimized for truth · quiet compass. Connected to Remembrance Garden, Founder Walk, Campus Evolution Engine, Company Genome, Leadership DNA, Company DNA, Knowledge Graph, Organizational Inheritance, Chief of Staff, Studio Intelligence. Core in **`src/studio-os-core/founders-promise/`**.
- **Executive Framework (M60):** `/admin/studio/executive-framework` — constitutional foundation for every AI executive inside Studio OS. Executive philosophy (expand capacity · founder decides · stewardship) · identity inheritance (Company/Leadership/Creative/Writing/Operational DNA · Founder\'s Promise · history · values) · executive standards · decision framework (11 impact dimensions) · executive collaboration · institutional memory · executive workspaces in Architect Studio · accountability metrics · recommendation pipeline (summary/reasoning/evidence/confidence/tradeoffs/alternatives) · leadership map · future executive roles (CBO/CEO/CDO/CGO/CFO/COO/etc.) · organizational priorities. Premium · calm · collaborative. Connected to Chief of Staff, all architects, Leadership DNA, Company DNA, Knowledge Graph, Company Genome, Studio Intelligence, Organizational Inheritance. Core in **`src/studio-os-core/executive-framework/`**.
- **Chief Brand Officer (M61):** `/admin/studio/chief-brand-officer` — lifelong guardian of organizational identity V2.0. Not logos — protect meaning across every touchpoint. Executive compass · brand governance · alignment engine · brand intelligence · evolution · brand council · creative review studio (Architect Studio atelier) · brand memory · protection · daily briefing · executive recommendations. Timelessness · trust · clarity · memorability · authenticity. Connected to Executive Framework, Brand/Experience/Digital/Growth architects, Company Genome, DNA layers, KG, Relationship Engine, Reader Graph, CoS, Studio Intelligence, Founder's Promise. Core in **`src/studio-os-core/chief-brand-officer/`**.
- **Chief Experience Officer (M62):** `/admin/studio/chief-experience-officer` — lifelong guardian of customer experience V2.0. Not interfaces — protect trust, delight, confidence, loyalty, and emotional connection across every touchpoint. Executive compass ("If someone experienced our company for the very first time today, would they understand who we are without us ever having to explain it?") · experience governance · alignment engine · journey intelligence · experience intelligence · evolution · experience council · experience studio (hospitality innovation lab in Architect Studio) · experience memory · protection · daily briefing · executive recommendations. Humanity over efficiency · hospitality · empathy · belonging. Connected to Executive Framework, Brand/Experience/Digital/Growth architects, Company Genome, DNA layers, KG, Relationship Engine, Reader Graph, CoS, Studio Intelligence, Founder's Promise, Chief Brand Officer. Core in **`src/studio-os-core/chief-experience-officer/`**.
- **Leadership Manifesto Framework (M60.5):** `/admin/studio/leadership-manifesto-framework` — constitutional foundation inherited by every executive V1.0. Shared leadership principles · discipline-specific expertise · living organizational DNA. Executive identity · leadership philosophy · core beliefs · non-negotiables · decision framework · executive compass (unique per discipline) · definition of excellence · communication standards · collaboration philosophy · learning philosophy · founder relationship · legacy commitment · manifesto inheritance for all current and future executives. Wisdom over authority · stewardship over control. Connected to Executive Framework, Leadership DNA, Company DNA, Founder's Promise, Company Genome, KG, Organizational Inheritance, CoS, Studio Intelligence, Executive Council, CBO, CEO. Core in **`src/studio-os-core/leadership-manifesto-framework/`**.
- **Chief Digital Officer (M63):** `/admin/studio/chief-digital-officer` — lifelong guardian of the digital ecosystem V1.0. Not writing code — protect architecture, design systems, platform consistency, automation, security, performance, accessibility, scalability. Executive compass ("Does this technology disappear into the experience, or does it become the experience?") · digital governance · alignment engine · digital intelligence · evolution · solution architecture · AI ecosystem · technology council · digital studio (product innovation lab in Architect Studio) · digital memory · protection · daily briefing · executive recommendations. Clarity · simplicity · longevity · craftsmanship. Connected to Executive Framework, Leadership Manifesto, Digital/Brand/Experience/Growth architects, Company Genome, DNA layers, KG, CoS, Studio Intelligence, Founder's Promise, CBO, CEO. Core in **`src/studio-os-core/chief-digital-officer/`**.
- **Chief Technology Officer (M63.5):** `/admin/studio/chief-technology-officer` — lifelong guardian of engineering organization & infrastructure V1.0. Not designing experiences — ensure technology remains resilient, secure, maintainable, scalable for decades. Executive compass ("Will this architecture still serve the organization ten years from now?") · technology governance · engineering alignment engine · engineering intelligence · evolution · platform architecture · engineering council · technology operations center (mission control in Architect Studio) · engineering memory · technology protection · daily briefing · executive recommendations. Built to last · never chase trends. Connected to Executive Framework, Leadership Manifesto, CDO, Digital Architect, Company Genome, Operational DNA, Leadership DNA, KG, CoS, Studio Intelligence, Founder's Promise. Core in **`src/studio-os-core/chief-technology-officer/`**.
- **Chief Growth Officer (M64):** `/admin/studio/chief-growth-officer` — lifelong guardian of sustainable organizational growth V1.0. Not vanity metrics — ensure every growth initiative strengthens brand, customer relationships, operational health, founder's promise, and long-term resilience. Executive compass ("Will this make the company bigger, or will it make the company better?") · growth governance · growth alignment engine · growth intelligence · growth evolution · growth laboratory (innovation lab in Architect Studio) · growth memory · growth protection · daily executive briefing · executive recommendations. Intentional · measurable · repeatable · ethical · relationship-driven. Connected to Executive Framework, Leadership Manifesto, Growth/Brand/Experience/Digital architects, Strategy/Campaign/Distribution engines, Reader Graph, Relationship Engine, Creator Marketplace, Knowledge Graph, Company Genome, Leadership DNA, CoS, Studio Intelligence, Founder's Promise, CBO, CEO, CDO, CTO. Core in **`src/studio-os-core/chief-growth-officer/`**.
- **Executive Council (M65):** `/admin/studio/executive-council` — highest collaborative leadership body V2.0. Every major decision evaluated through executive reasoning — not isolated recommendations. Council philosophy (healthy disagreement · respectful challenge · evidence-based stewardship) · executive council chamber in Architect Studio · council responsibilities · executive debate · healthy disagreement · Chief of Staff facilitation · decision synthesis · executive transparency · meeting modes · executive simulations · organizational learning · founder participation · council intelligence · leadership culture · executive council oath. Organizational wisdom over manufactured debate · founder remains final decision maker. Connected to Executive Framework, Leadership Manifesto, CoS, all C-suite executives, Company Genome, Knowledge Graph, Relationship Engine, Reader Graph, Studio Intelligence, Founder's Promise. Core in **`src/studio-os-core/executive-council/`**.
- **Organizational Intelligence (M66):** `/admin/studio/organizational-intelligence` — collective mind of every company V1.0. Not another AI assistant — continuously observes, learns, connects, reflects, predicts, strengthens every system. Intelligence philosophy (organizations learn like people · every event contributes · nothing forgotten) · continuous learning · organizational reasoning · cross-system intelligence · organizational curiosity · decision intelligence · organizational reflection · wisdom elevation (information → knowledge → understanding → wisdom) · institutional memory · organizational forecasting · intelligence center in Architect Studio · executive integration · founder intelligence. Never replaces founder judgment — expands human judgment · preserves institutional wisdom. Connected to Executive Council, Executive Framework, Leadership Manifesto, CoS, all C-suite, all architects, Company Genome, Knowledge Graph, Reader Graph, Relationship Engine, Strategy/Campaign/Distribution engines, Creator Marketplace, Founder's Promise, Organizational Inheritance, Studio Intelligence. Core in **`src/studio-os-core/organizational-intelligence/`**.
- **Organizational Autonomy Framework (M67):** `/admin/studio/organizational-autonomy-framework` — constitutional system governing how organizations safely execute work V1.0. Autonomy is trusted stewardship not independence — earned through trust · never speed over stewardship. Progressive autonomy levels 0–5 (observe → recommend → prepare → execute → cross-functional → stewardship) · autonomy governance (observe/recommend/prepare/schedule/publish/communicate/delegate/purchase/approve/escalate/pause) · founder permissions (always ask · threshold · automatic · never per domain) · trust engine (executive/workflow/decision/approval reliability · trust over time) · executive coordination · autonomous workflows · organizational transparency (reasoning · evidence · rollback · precedent) · autonomy dashboard · learning loop feeding OI · founder-reserved decisions always under founder authority. Ask "should the organization be trusted?" not "can AI do this?" Connected to OI, Executive Council, Executive Framework, Leadership Manifesto, CoS, all C-suite, Company Genome, KG, Relationship Engine, Organizational Inheritance, Studio Intelligence, Founder's Promise. Core in **`src/studio-os-core/organizational-autonomy-framework/`**.
- **Organizational Delegation Engine (M68):** `/admin/studio/organizational-delegation-engine` — outcome-based delegation V1.0. Founders delegate missions, objectives, and desired outcomes — not isolated tasks. Studio OS determines executive ownership, supporting executives, workflows, approvals, and success metrics. Delegation philosophy (define outcomes · organization determines how · increase capability not workload · never reduce visibility) · delegation types (projects · initiatives · campaigns · goals · strategic objectives · customer experiences · research · creative · operations · growth · improvements · long-term missions) · executive assignment with selection rationale · delegation planning (strategy · roadmap · dependencies · timeline · resources · risks · metrics · confidence · alternatives · founder review) · collaborative execution across C-suite · delegation governance inheriting OAF rules · visibility dashboard · organizational learning feeding OI · executive accountability · recommended delegations. Transform management into leadership · major strategic decisions remain founder authority. Connected to OAF, OI, Executive Council, CoS, all C-suite, Company Genome, KG, Relationship Engine, Strategy/Campaign/Distribution engines, Creator Marketplace, Founder's Promise, Studio Intelligence. Core in **`src/studio-os-core/organizational-delegation-engine/`**.
- **Organizational Workflow Orchestration (M69):** `/admin/studio/organizational-workflow-orchestration` — cross-functional workflow choreography V1.0. Coordinates executives, architects, departments, knowledge, relationships, and intelligence toward shared outcomes — coordinated teams not disconnected automations. Workflow philosophy (collaboration not tasks · multiple disciplines · living organization) · workflow types (product launches · campaigns · brand refreshes · onboarding · publishing · creator collaborations · affiliate · partnerships · research · feature development · support · community · knowledge · executive planning · improvements) · cross-functional orchestration with participant briefs · workflow intelligence (strategy · responsibilities · dependencies · knowledge · relationships · risks · timeline · resources · confidence · rationale) · living workflow engine (continuous adaptation) · Chief of Staff as conductor · workflow transparency · organizational self-adaptation · workflow memory · simulations (best/worst case · risks · customer impact · alternatives) · workflow dashboard. Coordinated simplicity · strengthen trust · collaboration · knowledge · customer outcomes · founder confidence. Connected to ODE, OAF, OI, Executive Council, CoS, all C-suite, all architects, Company Genome, KG, Reader Graph, Relationship Engine, Strategy/Campaign/Distribution engines, Creator Marketplace, Founder's Promise, Studio Intelligence. Core in **`src/studio-os-core/organizational-workflow-orchestration/`**.
- **Organizational Self-Improvement (M70):** `/admin/studio/organizational-self-improvement` — continuous organizational evolution V1.0. Organization reflects on performance, identifies opportunities, recommends enhancements, strengthens itself — learning compounds every day. Improvement philosophy (every event strengthens the org · culture not project · never change for change alone · master craftsman refinement) · continuous reflection across leadership/CX/brand/digital/tech/growth/relationships/knowledge/operations/community/creator/maturity · cross-functional improvements (executives recommend outside discipline) · improvement opportunities (why now · why it matters · impact · confidence) · organizational experiments (pilots · A/B · safe proposals · institutional knowledge) · improvement governance (automatic · thresholds · experimentation permissions · structural changes founder-only) · continuous learning capture · organizational maturity over years · Chief of Staff coordinates all improvement initiatives · improvement dashboard. Strengthen clarity · trust · relationships · knowledge · leadership · CX · resilience. Connected to OWF, ODE, OAF, OI, Executive Council, CoS, all C-suite, all architects, Company Genome, KG, Reader Graph, Relationship Engine, Strategy/Campaign/Distribution engines, Creator Marketplace, Founder's Promise, Studio Intelligence, Organizational Governance & Safeguards. Core in **`src/studio-os-core/organizational-self-improvement/`**.
- **Organizational Governance & Safeguards (M71):** `/admin/studio/organizational-governance-safeguards` — constitutional governance and invisible safeguards V1.0. Highest governing document inside Studio OS — every autonomous action, executive recommendation, workflow, and organizational decision evaluated against founder promise, values, legal responsibilities, ethical standards, and long-term stewardship. Governance philosophy (defined by what we refuse to compromise · preserve trust while enabling progress · invisible stewardship not bureaucracy · never grow faster than values) · living organizational constitution (founder's promise · company DNA · leadership DNA · values · executive manifesto · decision principles · governance rules · ethical standards · institutional memory) · governance policies (communication · customer · privacy · security · financial · brand · publishing · partnerships · technology · AI · knowledge · creator · community · vendor · international · employment · custom rules) · decision safeguards (pre-execution evaluation · approval status · risk level · confidence · recommended action) · executive guardians (every executive protects their discipline) · organizational ethics (principles · boundaries · commitments) · risk intelligence (legal · security · privacy · brand · customer · financial · operational · knowledge · relationship · reputation · drift) · governance simulations · approval framework (automatic · executive · council · founder · board · auto-escalation) · organizational transparency (reasoning · executives · evidence · approval pathway · policies · outcomes) · governance dashboard · continuous governance evolution. Trust becomes institutional · competitive advantage. Connected to OSI, OWF, ODE, OAF, OI, Executive Council, Executive Framework, Leadership Manifesto, CoS, all C-suite, Company Genome, KG, Relationship Engine, Founder's Promise, Organizational Inheritance, Studio Intelligence, Organizational Maturity Model. Core in **`src/studio-os-core/organizational-governance-safeguards/`**.
- **Organizational Maturity Model (M72):** `/admin/studio/organizational-maturity-model` — master progression system V1.0. Governs every company inside Studio OS by understanding where each organization exists in its journey and adapting recommendations, executives, autonomy, architecture, workflows, governance, and intelligence accordingly. Maturity philosophy (organizations mature not simply scale · recognize maturity not size · readiness across leadership/knowledge/operations/relationships/customer/brand/technology/financial/community · earned not unlocked · never revenue alone) · organizational stages (idea · validation · launch · growth · scale · enterprise · holding company · legacy) · adaptive experience (UI complexity · executive participation · workflows · autonomy · governance · reporting · dashboards · architect studios · campus · OI adapt to maturity) · executive readiness (recommend executives only when maturity supports · CoS/Business Architect at idea · Brand/Experience at validation · CBO/CEO at launch · CDO/CGO at growth · CTO/Council at scale · portfolio at holding company) · autonomy progression (observation → guided → assisted → trusted → cross-functional → stewardship · display why levels change) · campus progression (founder studio → innovation loft → creative HQ → executive HQ → innovation campus → global campus → organizational institute → legacy campus) · organizational assessments (10 domains · maturity score · confidence · strengths · opportunities · next milestone) · growth roadmap (current/next stage · requirements · priorities · readiness · dependencies · future executives) · company onboarding (assess existing orgs · generate genome · KG · maturity · executive recommendations · campus starting point) · OI integration (evaluate evolution · slow down/accelerate/strengthen before advancing). Master operating framework — every recommendation begins with "is the organization ready?" Connected to OGS, OSI, OWF, ODE, OAF, OI, Executive Council, all C-suite, all architects, Company Genome, Campus Evolution Engine, KG, Relationship Engine, Founder's Promise, Studio Intelligence, Company Maturity Engine, Leadership Modes. Core in **`src/studio-os-core/organizational-maturity-model/`**.
- **Leadership Modes (M73):** `/admin/studio/leadership-modes` — founder & executive mode V1.0. Studio OS adapts to the founder's current leadership mindset, priorities, decision-making style, and objectives. Leadership philosophy (founders wear different hats · adapt to founder not force founder to adapt · same org/knowledge/executives/campus · perspective changes not fragmentation) · founder mode (creation · vision · innovation · strategy · brand · architecture · minimize operational noise) · executive mode (leadership · briefings · health · council · approvals · governance · organizational awareness) · creator mode (content · publishing · campaigns · NDXBOOK · reader graph · creator marketplace · media) · operator mode (execution · workflows · delegations · coordination · bottlenecks · momentum) · automatic mode detection (Studio Intelligence recommends · override anytime) · adaptive interface (navigation · dashboard · briefings · notifications · shortcuts · widgets · campus · familiar shell different priorities) · Chief of Staff mode-specific briefings (vision · leadership · creative · execution) · executive behavior adapts (possibilities · decisions · inspiration · execution) · OI continuous mode evaluation · campus ambient transformation · instant leadership transitions with zero context lost. "How do you want to lead today?" Connected to OMM, OGS, OSI, OWF, ODE, OAF, OI, Executive Council, CoS, all C-suite, all architects, Company Genome, Campus Evolution Engine, Founder's Promise, Studio Intelligence. Core in **`src/studio-os-core/leadership-modes/`**.
- **Creative Preview (designer showcase):** Preview deployments only. Secret URL **`?creativePreview=<VITE_CREATIVE_PREVIEW_TOKEN>`** (env set on Vercel **Preview** only). **`isCreativePreviewMode()`** in **`src/utils/creativePreviewMode.ts`** — seeds demo premium/BLACK member, bypasses sign-in/premium guards, blocks checkout payments; banner **`CreativePreviewBanner`**. Docs: **`docs/CREATIVE_PREVIEW.md`**. Not the same as **`/debug-mode`** (founder layout editor).
- **NOIR BAW live WebP previews:** **`resolveAdminNoirHubLiveWigViewsFromStorage`** returns live triples only on NOIR **step** routes: **`/build-a-wig/noir/customize/<step>`** and **`/build-a-wig/noir/edit/<step>`** (`isNoirBawLivePreviewStepPathname`). **Not** on **`/build-a-wig/noir`**, **`/build-a-wig/noir/customize`**, or **`/build-a-wig/noir/edit`** — those **`BuildAWigPage`** hubs always use **static** `/assets/` mannequins + brick for hero + thumbs. **Hub thumb routes** (outer **`.leaf-bg`** + static mannequin `<img>` — no inner CSS brick): **`/build-a-wig`**, **`/build-a-wig/{unit}`**, **`/build-a-wig/{unit}/customize`**, **`/build-a-wig/{unit}/edit`** for units noir/blanco/soft-wave/beach-wave/soft-curl/ocean-curl — **not** deeper **`/customize/color`** etc. **`.leaf-stack.thumb`** uses **`isolation: isolate`** so **`.leaf-bg`** (`z-index: -1`) still paints inside the stack. Hub thumb wrapper uses **`p-0`** (not **`p-1`**) so the **72×95** frame aligns with **`.leaf-bg` (95px)** — padding was pushing the mannequin below the visible brick. **BAW product hub** three-thumb row uses **12px** gap (same as NOIR customize sub-pages with live previews in **`BawNoirWigPreviewHeroThumbs`**); static-only **deeper** sub-pages keep **2px** gap + per-frame nudge. **Static** thumbs anchor the mannequin **to the bottom** (`.baw-noir-thumb-static-img`). **Sub-page live** NOIR hero uses the **same** brick cell as static (**282×387** mannequin vars), not the old **262×367** live box. **Sub-page live** NOIR thumbs: **72×95** frame (same height as hub static), **`object-fit: cover`** on the WebP so the cell fills like the hub (no gray/white side letterboxing from `contain`); **static** sub-page thumb `<img>` dimensions match **72×95** (~**15%** larger than prior **63×84**). **NOIR color** sub-page **OFF BLACK** (static naturals): **`thumbSpacingLikeSubLive`** on **`BawNoirWigPreviewHeroThumbs`** so **12px** gap matches hub + live sub-pages. **Storage layout:** offline batch uploads use **`wig-preview/{v}/NOIR/{hash}.webp`** (single combined asset per manifest row); the live color API caches **three angles** under **`wig-preview-live/{v}/NOIR/{colorTierHash}/{left|front|right}.webp`** where **`colorTierHash`** forces **`styling: NONE`** in the hash; **OFF BLACK** → **`OFF_BLACK`**; **JET BLACK** → **`JET_BLACK_OFF_BLACK`** (legacy Storage path) — so **pregenerated batch files do not satisfy** the live-color path; the API skips Fal only when those **live** objects exist. **Not** admin-email-gated; **all users** see committed/pending live WebPs when selecting options. **Customize draft vs hub:** On **`/build-a-wig/noir/customize/*`** sub-pages, WIP choices use **`customizeSelected*`** (and **`customizeSelectedHairStyling`** / **`customizeSelectedPartSelection`** for layers/part); **`selected*`** hub keys update on **Confirm** (or edit flow). Live preview **color** on steps **other than** **`.../customize/color`** resolves **`selectedColor`** first (confirmed), then **`customizeSelectedColor`** (draft); **`bawNoirLiveColorWigViewsPending`** applies only on the **color** sub-page. **Styling** and **hairline** NOIR customize sub-pages **persist `customizeSelected*` on every tap** (not only Back/Confirm) so preview resolution never falls back to stale **`selected*`** mid-draft. Exact **`/build-a-wig/noir/customize`** hub uses confirmed **`customizeSelected*`** only for live preview resolution — not in-flight draft taps. **NOIR color sub-page (`.../noir/customize/color`, `.../noir/edit/color`):** `resolveAdminNoirHubLiveWigViewsFromStorage` returns **only** color-tier live WebPs (`pending` + committed) — **not** salon/bangs styling triples — so swatch taps update the hero. Other NOIR step routes still composite styling/bangs when selected. Selecting a paid color **no longer clears** `bawNoirLiveColorWigViewsPending` before the next triple resolves — **optimistic** hash-based public URLs **first**, optional **single-`HEAD`** verify (`front.webp` only), then **`customStorageChange`**. The page **holds** the last good live triple in state while the next color resolves so the hero does **not** flash to naturals; **OFF BLACK** uses the **same** `wig-preview-live` Fal triples as other swatches (**not** forced static `/assets/` naturals — **`shouldUseCommittedBawNoirLiveColorWigViews`** includes **OFF BLACK**). **`POST /api/wig-preview/live-noir-color`:** any **signed-in** user may call — **missing** angles only unless **`forceRegenerate: true`** (re-runs Fal even when WebPs exist). **NOIR color** page: premium gate + **`liveWigViews`** drives the hero; Fal regen strip + **regen all** / **L-M-R** controls are **founder-only in the DOM** (`display: none`, `aria-hidden`) so text is not visible but tooling / parity with the old flow remains. **`POST /api/live-wig-after-color-styling`:** **`forceRegenerate`** is not founder-only (any signed-in session with Bearer for **new** Fal runs). If **`!forceRegenerate`** and all **`after-color/***` outputs already exist in Storage, the API returns **`publicUrls`** immediately (**`cacheOnly: true`**) **without** requiring **`Authorization`** — matches warm-cache styling preview when no Fal runs. **NOIR styling** page (`.../noir/edit/styling`, `.../noir/customize/styling`): live preview Fal calls use **`isPremiumMemberForGatedFeatures()`**; Fal / LIVE PREVIEW / regen **text** is **visible** on these routes only (still hidden on other NOIR BAW steps). **NOIR hairline** (`.../hairline`): founder hairline hint text shows only on these routes. **Showing** the Fal regen strip on **other** NOIR steps (styling regen, hairline hint, etc.) still requires **`isFounderNoirFalRegenUiVisible()`** — signed in **and** **`currentUser`** email is founder Gmail; other signed-in accounts never see those controls (they still see live WebPs from storage when applicable). Fal API still needs Bearer. **Live LAYERS / CRIMPS / FLAT IRON (after-color styling):** **`POST /api/live-wig-after-color-styling`** accepts **exactly one** salon style among **LAYERS**, **CRIMPS**, **FLAT IRON**. All use **color-tier WebPs** from Storage (same input as bangs) + **`buildLayersStylePromptFromColorTierWebp`**, **`buildCrimpsStylePromptFromColorTierWebp`**, or **`buildFlatIronStylePromptFromColorTierWebp`** so **hair stays the selected swatch** (not black from HQ brick refs); **MIDDLE + FLAT IRON** (no bangs) may use a **second** fal **`image_url`** (public **`/assets/natural {left|front|right}.png`**) via **`WIG_PREVIEW_PUBLIC_APP_ORIGIN`** / **`SITE_URL`** when Fal runs — **but the styling step UI** shows **`currentNoirColorTripleOrNull`** (same **color-tier** `wig-preview-live/...` as the NOIR color page), **not** `after-color/flat-iron-middle-part` WebPs (`resolveAdminNoirHubLiveWigViewsFromStorage` + **`styling/page.tsx`** skip salon storage for that combo). Prompts include **`STYLE LOCK`**: same canonical silhouette/texture per salon mode + part + angle across **all** catalog colors — **only** pigment follows the swatch; ignore input WebP shape drift between colors. Side parts (**UI L** / **UI R**) also get **`PART OVERRIDE`**: the color-tier WebP often bakes in a **default** part (often **image RIGHT** / UI L) — prompts tell Fal to **re-part** per customer UI, **not** preserve that reference part line. **UI R** + **LAYERS** or **CRIMPS** (with or without bangs): **`POST /api/live-wig-after-color-styling`** uses the **already-generated MIDDLE-part** after-color WebP for that angle as the **single Fal input** (not the raw color-tier WebP). Prompt **`buildUiRightSalonFromMiddlePartOutputPrompt`** chains **INPUT** + **TASK** (UI R re-part) + style-keep + **Camera** + full **`DRAPE SIDE`** + optional **bangs** + **`bawFalEditPreserveReferenceBlock()`** + logo legibility / output-quality lines — **one** `image_urls` attachment only. **MIDDLE** outputs for that style must exist in Storage first (generate **MIDDLE** part, then **RIGHT**). LAYERS = **voluminous layered S-waves** (blended, brushed-out barrel curls — not separated ringlets); CRIMPS = **horizontal accordion crimps**; **FRONT** = **one-shoulder drape**; **part direction** in prompts uses **image LEFT** / **image RIGHT** (thumbnail) for **where the part sits** — **UI L** = part on **image RIGHT**; **UI R** = part on **image LEFT** (opposite of **UI L**); prompts use the **same** `partSelection` as the API (no L↔R swap in text). **API `partSelection`**, Storage paths, and filenames stay the **real** customer selection; **`DRAPE SIDE`** block: long hair drapes **only** over the **viewer’s LEFT** shoulder (left side of the image, “facing” the mannequin) for **MIDDLE** / **L** / **R** — UI **L** still parts on **image RIGHT** scalp with length swept to **viewer’s LEFT** shoulder; **forbidden** thick drape on **viewer’s RIGHT** shoulder; prompts ask for **shoulder still visible** through the hair (not an opaque blanket); **BANGS + MIDDLE** still requires asymmetric lengths below bangs; FLAT IRON = **bone-straight / flat-ironed** hair + **part direction only** (same base color shot, no new curl/crimp texture). **FLAT IRON + UI LEFT:** **`POST /api/live-wig-after-color-styling`** may set **`publicUrls.right`** (R / right-camera thumbnail) to the **same URL** as **`flat-iron-right-part/right.webp`** when that object exists — so the R thumb matches the **RIGHT**-part flat-iron asset; **`outputPaths.right`** still refers to **`flat-iron-left-part`**. Storage: **`after-color/layers-*-part/`**, **`after-color/crimps-*-part/`**, **`after-color/flat-iron-*-part/`**. **BANGS + salon:** same API with **`styling`** containing both — salon prompt + **`includeBangs`** (curtain bangs per **part**); storage **`layers-with-bangs-*-part/`** / **`crimps-with-bangs-*-part/`** / **`flat-iron-with-bangs-*-part/`**; localStorage **`salonMode`** **`LAYERS_BANGS`** / **`CRIMPS_BANGS`** / **`FLAT_IRON_BANGS`**. Requires color previews generated first. **Styling step (`/build-a-wig/.../styling`):** whenever **BANGS** is selected (**alone** or with **LAYERS** / **CRIMPS** / **FLAT IRON**), **PART** is **MIDDLE only** — **LEFT/RIGHT** disabled (state forced to **MIDDLE**). **BANGS-only** live preview still requires **color-tier WebPs** in Storage (single attachment). **Styling sub-page auto-preview** calls **`POST /api/live-wig-after-color-styling`** **without** **`forceRegenerate`** so existing **`after-color/***` WebPs are **skipped** (no Fal) when revisiting a part; founder **regen** buttons send **`forceRegenerate: true`**. On the **styling** step, founder live state **does not** clear salon/bangs triples on Storage cache miss or on failed/empty fetch — the **last successful** triple stays visible until the new one loads (stale-while-revalidate; avoids flashing base/color naturals). **Fal prompt fidelity:** shared anti–plastic-drift / preserve-reference lines live in **`api/_lib/bawFalEditFidelityPrompt.ts`** (duplicated inline in **`api/wig-preview/live-noir-color.ts`** because nested API routes cannot import `_lib` on Vercel); styling and send-offer chain prompts import the same module.
