# Constitution / articles

Data-driven constitutional article payloads. Use JSON matching `RegisterConstitutionArticleInput` and ingest via `ingestConstitutionArticlePayload()` — no engineering changes required.

See `article.schema.json` for the payload schema.
