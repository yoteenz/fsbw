# Visual Snapshots — Build-A-Wig

Color variant assets derived from approved master hero portraits.

## Naming

`{unitSlug}_{colorSlug}_{suffix}`

Suffixes: `hero`, `cart`, `wishlist`, `checkout`, `order`, `admin`

## Folder layout

```
visual-snapshots/signature-collection/{unitSlug}/{colorSlug}/
```

Published copies: `/assets/baw-visual-snapshots/{assetId}.png`

Assets are not auto-generated in Milestone 21.5 — architecture and lookup only.
