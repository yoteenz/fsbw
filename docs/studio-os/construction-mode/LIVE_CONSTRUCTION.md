# Live Construction

When Compile begins, founder watches construction progress per job with progress bars.

No mystery. Everything visible.

```typescript
updateLiveConstructionStage(view, jobId, 'inspecting', 80, 'Inspecting');
```
