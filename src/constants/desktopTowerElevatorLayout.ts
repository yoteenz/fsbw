/** Percentage layout zones on the elevator shell artboard (1536×1024). */
export const TOWER_SHELL_HOLO = {
  top: 9,
  width: 30,
} as const;

/** In-cabin floor indicator column (right glass panel). */
export const TOWER_SHELL_FLOOR_SHAFT = {
  top: 20,
  right: 7.5,
  width: 11,
  bottom: 30,
} as const;

export const TOWER_SHELL_FLOOR_MARKER_STEP_PX = 56;
