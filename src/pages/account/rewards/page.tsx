/**
 * Account → Rewards (`/account/rewards`).
 * Implementation lives in `membership/page.tsx` (legacy path); this module is the canonical import for the rewards route.
 */
export { default } from '../membership/page';
