/**
 * Vertical offset from viewport top to the admin main hub card (meetings, pending, etc.)
 * and to header messages/notifications dropdown panels.
 *
 * Must match `AdminHeader`: `py-4` (16px) + inner bar `h-10` (40px) + `py-4` (16px) = 72px.
 */
export const ADMIN_MAIN_CARD_TOP_OFFSET_PX = 72;
