/** Milestone 105 — Knowledge Confidence™ bootstrap (org-scoped profiles created on first access). */

export function bootstrapKnowledgeConfidencePlatform(): void {
  /* Profiles sync on ensureOrganizationKnowledgeConfidenceProfile — no global seed required. */
}
