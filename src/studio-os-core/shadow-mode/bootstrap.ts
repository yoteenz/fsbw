/** Milestone 102 — Shadow Mode™ bootstrap (org-scoped profiles created on first access). */

export function bootstrapShadowModePlatform(): void {
  /* Profiles sync on ensureOrganizationShadowModeProfile — no global seed required. */
}
