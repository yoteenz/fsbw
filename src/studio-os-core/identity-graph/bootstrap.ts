/** Identity Graph™ — no global bootstrap required; profiles sync per organization. */
export function bootstrapIdentityGraphStore(): void {
  /* org-scoped via ensureOrganizationIdentityGraphProfile */
}
