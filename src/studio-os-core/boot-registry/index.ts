/** BootRegistry™ public surface — register and inspect boot modules. */
export { bootRegistry, createBootModule } from '../kernel/boot-registry';
export { registerAllStudioBootModules } from '../bootstrap/register-boot-modules';
export { STUDIO_BOOT_ORDER } from '../kernel/studio-kernel';
