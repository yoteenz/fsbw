/** Milestone 101 — Wisdom Capture™ bootstrap (org-scoped profiles created on first access). */

export function bootstrapWisdomCapturePlatform(): void {
  /* Profiles sync on ensureOrganizationWisdomProfile — no global seed required. */
}
