import { ArchiveVaultMedia } from './ArchiveVaultMedia';

/** Acrylic preservation housing — ambient material shell, pointer-events none. */
export function ArchiveVaultShell() {
  return <ArchiveVaultMedia motionActive />;
}
