/** Canonical SITE 00 Origin desktop artboard — composition validated @ 1440px. */
export const SITE00_DESKTOP_ARTBOARD_WIDTH = 1440;

/** Minimum stage height inside the artboard (content may grow taller). */
export const SITE00_DESKTOP_ARTBOARD_MIN_HEIGHT = 900;
