{
  "name": "Build-a-Wig mobile preview",
  "install": "./scripts/cloud-update.sh",
  "terminals": [
    {
      "name": "vite",
      "command": "FSBW_CLOUD_MOBILE_PREVIEW=1 npm run dev",
      "description": "Vite dev server on port 3001 — live UI; /api proxied to production"
    },
    {
      "name": "preview-tunnel",
      "command": "./scripts/cloud-preview-tunnel.sh",
      "description": "Cloudflare tunnel — persistent URL when CLOUDFLARE_TUNNEL_TOKEN is set; else trycloudflare.com"
    }
  ],
  "ports": [
    {
      "name": "vite",
      "port": 3001
    }
  ]
}
